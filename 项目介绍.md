# AI 加密货币自动交易系统 - 项目介绍

## 项目概述

这是一个基于 **VoltAgent 框架** 构建的 AI 驱动加密货币自动交易系统，支持 **Gate.io** 和 **OKX** 交易所。系统通过集成多个 AI 模型（DeepSeek、Grok、Claude 等）进行市场分析和交易决策，实现最小化人工干预的自动化交易。

### 核心特性
- **AI 智能决策**：集成多种 AI 模型进行市场分析和交易决策
- **多交易所支持**：支持 Gate.io 和 OKX 交易所
- **实时监控**：Web 实时仪表盘监控系统状态
- **风险管理**：完善的风险管理系统，包括止损、止盈、移动止盈等
- **多策略支持**：提供多种交易策略（保守、平衡、激进等）
- **数据持久化**：完整的数据库记录交易历史和系统状态

## 技术架构

### 技术栈
- **后端框架**：VoltAgent（AI Agent 框架）
- **运行时**：Node.js + TypeScript
- **数据库**：SQLite（通过 LibSQL）
- **Web 服务器**：Deno（用于 API 服务）
- **交易所 API**：Gate.io API、OKX API
- **AI 模型**：OpenAI API（支持 DeepSeek、Grok、Claude 等）

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         Web Dashboard                        │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                         API Server                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Trading Loop │ │   Monitors   │ │   Database   │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
       ▼                ▼                │
┌──────────────┐ ┌──────────────┐        │
│ Trading Agent│ │ Risk Manager │        │
└──────┬───────┘ └──────┬───────┘        │
       │                │                │
       ▼                ▼                │
┌────────────────────────────────────────┴─────────────────┐
│                    Exchange Client                         │
│              (Gate.io / OKX)                               │
└────────────────────────────────────────────────────────────┘
```

## 核心代码结构

```
src/
├── index.ts                    # 应用入口点
├── agents/                     # AI Agent 定义
│   └── tradingAgent.ts         # 交易 Agent 实现
├── scheduler/                  # 调度器
│   └── tradingLoop.ts          # 交易循环调度
├── strategies/                 # 交易策略
│   ├── index.ts                # 策略导出和管理
│   ├── conservative.ts         # 保守策略
│   ├── balanced.ts             # 平衡策略
│   ├── aggressive.ts           # 激进策略
│   └── ...                     # 其他策略
├── tools/                      # Agent 工具
│   └── trading/                # 交易相关工具
│       ├── marketData.ts       # 市场数据工具
│       ├── tradeExecution.ts   # 交易执行工具
│       └── accountManagement.ts # 账户管理工具
├── services/                   # 服务层
│   ├── exchangeClient.ts       # 交易所客户端
│   ├── marketDataService.ts    # 市场数据服务
│   └── tradeExecutionService.ts # 交易执行服务
├── monitors/                   # 监控器
│   ├── accountRecorder.ts      # 账户记录器
│   ├── trailingStop.ts         # 移动止盈监控器
│   ├── stopLoss.ts             # 止损监控器
│   └── partialProfit.ts        # 分批止盈监控器
├── database/                   # 数据库
│   ├── schema.ts               # 数据库 Schema
│   └── queries.ts              # 数据库查询
├── api/                        # API 路由
│   └── routes.ts               # API 端点定义
├── config/                     # 配置
│   ├── riskParams.ts           # 风险参数配置
│   └── tradingConfig.ts        # 交易配置
└── utils/                      # 工具函数
    ├── logger.ts               # 日志工具
    └── indicators.ts           # 技术指标计算
```

## 核心模块详解

### 1. 应用入口 (index.ts)

主入口文件负责初始化系统各个组件：

```typescript
async function main() {
  logger.info("启动 AI 加密货币自动交易系统");
  
  // 1. 初始化数据库
  logger.info("初始化数据库...");
  await initDatabase();
  
  // 2. 初始化交易系统配置
  await initTradingSystem();
  
  // 3. 启动 API 服务器
  logger.info("🌐 启动 Web 服务器...");
  const apiRoutes = createApiRoutes();
  
  const port = Number.parseInt(process.env.PORT || "3141");
  
  server = serve({
    fetch: apiRoutes.fetch,
    port,
  });
  
  // 4. 启动交易循环
  logger.info("启动交易循环...");
  startTradingLoop();
  
  // 5. 启动账户资产记录器
  logger.info("启动账户资产记录器...");
  startAccountRecorder();
  
  // 6. 启动移动止盈监控器
  logger.info("启动移动止盈监控器...");
  startTrailingStopMonitor();
  
  // 7. 启动止损监控器
  logger.info("启动止损监控器...");
  startStopLossMonitor();
  
  // 8. 启动分批止盈监控器
  logger.info("启动分批止盈监控器...");
  startPartialProfitMonitor();
}
```

### 2. 交易 Agent (tradingAgent.ts)

核心 AI 交易 Agent，负责市场分析和交易决策：

```typescript
export function createTradingAgent() {
  const agent = new Agent({
    name: "TradingAgent",
    model: createOpenAI({
      apiKey: process.env.OPENAI_API_KEY,
      baseURL: process.env.OPENAI_BASE_URL,
    }),
    tools: {
      // 市场数据工具
      getMarketPrice: getMarketPriceTool(),
      getTechnicalIndicators: getTechnicalIndicatorsTool(),
      getFundingRate: getFundingRateTool(),
      
      // 交易执行工具
      openPosition: openPositionTool(),
      closePosition: closePositionTool(),
      
      // 账户管理工具
      getAccountBalance: getAccountBalanceTool(),
      getPositions: getPositionsTool(),
    },
    memory: new Memory({
      adapter: new LibSQLMemoryAdapter(dbClient),
    }),
  });
  
  return agent;
}
```

### 3. 交易循环 (tradingLoop.ts)

定时执行交易分析和决策：

```typescript
cron.schedule(`*/${intervalMinutes} * * * *`, async () => {
  // 1. 收集市场数据（多时间框架）
  const marketData = await collectMarketData();
  
  // 2. 获取账户信息
  const accountInfo = await getAccountInfo();
  
  // 3. 获取当前持仓
  const positions = await getPositions();
  
  // 4. 获取历史交易记录
  const tradeHistory = await getTradeHistory();
  
  // 5. 创建 AI Agent
  const agent = createTradingAgent();
  
  // 6. 生成交易提示词
  const prompt = generateTradingPrompt({
    marketData,
    accountInfo,
    positions,
    tradeHistory,
  });
  
  // 7. AI 分析并决策
  const result = await agent.run(prompt);
  
  // 8. 执行交易决策
  await executeDecision(result);
});
```

### 4. 交易策略 (strategies/index.ts)

提供多种交易策略配置：

```typescript
export function getStrategyParams(strategy: TradingStrategy, maxLeverage: number): StrategyParams {
  switch (strategy) {
    case "ultra-short":
      return getUltraShortStrategy(maxLeverage);
    case "swing-trend":
      return getSwingTrendStrategy(maxLeverage);
    case "medium-long":
      return getMediumLongStrategy(maxLeverage);
    case "conservative":
      return getConservativeStrategy(maxLeverage);
    case "balanced":
      return getBalancedStrategy(maxLeverage);
    case "aggressive":
      return getAggressiveStrategy(maxLeverage);
    case "aggressive-team":
      return getAggressiveTeamStrategy(maxLeverage);
    case "rebate-farming":
      return getRebateFarmingStrategy(maxLeverage);
    case "ai-autonomous":
      return getAiAutonomousStrategy(maxLeverage);
    case "multi-agent-consensus":
      return getMultiAgentConsensusStrategy(maxLeverage);
    case "alpha-beta":
      return getAlphaBetaStrategy(maxLeverage);
    default:
      return getAiAutonomousStrategy(maxLeverage);
  }
}
```

### 5. 数据库 Schema (database/schema.ts)

定义数据库表结构：

```typescript
export const CREATE_TABLES_SQL = `
-- 交易记录表
CREATE TABLE IF NOT EXISTS trades (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  order_id TEXT NOT NULL,
  symbol TEXT NOT NULL,
  side TEXT NOT NULL,
  type TEXT NOT NULL,
  price REAL NOT NULL,
  quantity REAL NOT NULL,
  leverage INTEGER NOT NULL,
  pnl REAL,
  fee REAL,
  timestamp TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending'
);

-- 持仓表
CREATE TABLE IF NOT EXISTS positions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  symbol TEXT NOT NULL UNIQUE,
  quantity REAL NOT NULL,
  entry_price REAL NOT NULL,
  current_price REAL NOT NULL,
  liquidation_price REAL NOT NULL,
  unrealized_pnl REAL NOT NULL,
  leverage INTEGER NOT NULL,
  side TEXT NOT NULL,
  profit_target REAL,
  stop_loss REAL,
  tp_order_id TEXT,
  sl_order_id TEXT,
  entry_order_id TEXT NOT NULL,
  opened_at TEXT NOT NULL,
  confidence REAL,
  risk_usd REAL,
  peak_pnl_percent REAL DEFAULT 0,
  partial_close_percentage REAL DEFAULT 0
);

-- 账户历史表
CREATE TABLE IF NOT EXISTS account_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  timestamp TEXT NOT NULL,
  total_balance REAL NOT NULL,
  available_balance REAL NOT NULL,
  unrealized_pnl REAL NOT NULL,
  position_count INTEGER NOT NULL
);

-- AI 决策记录表
CREATE TABLE IF NOT EXISTS ai_decisions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  timestamp TEXT NOT NULL,
  symbol TEXT NOT NULL,
  action TEXT NOT NULL,
  confidence REAL NOT NULL,
  reasoning TEXT,
  executed BOOLEAN DEFAULT FALSE
);
`;
```

## 交易流程

### 完整交易流程

1. **数据收集阶段**
   - 从交易所获取实时市场数据
   - 计算技术指标（EMA、MACD、RSI、ATR 等）
   - 获取账户余额和持仓信息
   - 获取历史交易记录

2. **AI 分析阶段**
   - 将市场数据、账户信息、持仓信息、历史记录输入 AI Agent
   - AI Agent 使用工具获取额外数据
   - AI 分析市场趋势并做出交易决策

3. **决策执行阶段**
   - 根据策略参数验证决策
   - 执行开仓/平仓操作
   - 设置止盈止损订单
   - 记录交易到数据库

4. **风险监控阶段**
   - 实时监控持仓盈亏
   - 触发止损时自动平仓
   - 触发止盈时执行止盈策略
   - 移动止盈跟踪价格变化

### 风险管理机制

- **止损保护**：每个持仓都有止损订单
- **止盈策略**：支持固定止盈和移动止盈
- **分批止盈**：可以分多个批次止盈
- **仓位控制**：根据风险参数控制仓位大小
- **杠杆限制**：根据策略限制最大杠杆

## 开发快速上手

### 环境准备

1. **安装依赖**
```bash
npm install
```

2. **配置环境变量**
创建 `.env` 文件：
```env
# 交易所 API
EXCHANGE=gate.io  # 或 okx
GATE_API_KEY=your_api_key
GATE_API_SECRET=your_api_secret
OKX_API_KEY=your_api_key
OKX_API_SECRET=your_api_secret
OKX_PASSPHRASE=your_passphrase

# AI 模型配置
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://api.deepseek.com/v1  # 或其他 AI 服务

# 交易配置
TRADING_SYMBOL=BTC_USDT
TRADING_STRATEGY=balanced
MAX_LEVERAGE=10
RISK_PERCENTAGE=2

# 系统配置
PORT=3141
LOG_LEVEL=info
```

3. **启动系统**
```bash
npm start
```

### 开发调试

1. **查看日志**
```bash
# 实时查看日志
npm run dev

# 查看特定模块日志
npm run dev 2>&1 | grep "TradingLoop"
```

2. **访问 Web 仪表盘**
打开浏览器访问：`http://localhost:3141`

3. **数据库查询**
```bash
# 使用 SQLite 客户端查看数据库
sqlite3 trading.db

# 查看持仓
SELECT * FROM positions;

# 查看交易记录
SELECT * FROM trades ORDER BY timestamp DESC LIMIT 10;
```

### 常用命令

```bash
# 启动系统
npm start

# 开发模式（带热重载）
npm run dev

# 运行测试
npm test

# 代码检查
npm run lint

# 类型检查
npm run typecheck

# 构建项目
npm run build
```

## 调试与监控

### 日志系统

系统使用结构化日志，包含以下级别：
- `info`：一般信息
- `warn`：警告信息
- `error`：错误信息
- `debug`：调试信息

日志会输出到控制台和文件（如果配置）。

### 监控指标

系统提供以下监控指标：
- 账户总余额
- 可用余额
- 未实现盈亏
- 持仓数量
- 交易成功率
- AI 决策置信度

### API 端点

主要 API 端点：
- `GET /api/status` - 系统状态
- `GET /api/positions` - 当前持仓
- `GET /api/trades` - 交易记录
- `GET /api/account` - 账户信息
- `POST /api/stop` - 停止系统
- `POST /api/start` - 启动系统

## 常见问题

### Q1: 如何切换交易策略？

修改 `.env` 文件中的 `TRADING_STRATEGY` 参数：
```env
TRADING_STRATEGY=conservative  # 可选: conservative, balanced, aggressive, etc.
```

### Q2: 如何调整风险参数？

修改 `src/config/riskParams.ts` 文件中的参数：
```typescript
export const RISK_PARAMS = {
  maxPositionSize: 0.1,      // 最大仓位比例
  maxLeverage: 10,           // 最大杠杆
  stopLossPercent: 0.02,     // 止损百分比
  takeProfitPercent: 0.04,   // 止盈百分比
  // ... 其他参数
};
```

### Q3: 如何添加新的 AI 模型？

在 `src/agents/tradingAgent.ts` 中修改模型配置：
```typescript
const agent = new Agent({
  model: createOpenAI({
    apiKey: process.env.OPENAI_API_KEY,
    baseURL: "https://api.your-model-provider.com/v1",
  }),
  // ... 其他配置
});
```

### Q4: 如何添加新的技术指标？

在 `src/utils/indicators.ts` 中添加新的指标计算函数：
```typescript
export function calculateCustomIndicator(data: number[]): number {
  // 实现你的指标计算逻辑
  return result;
}
```

### Q5: 如何自定义交易策略？

在 `src/strategies/` 目录下创建新的策略文件：
```typescript
export function getCustomStrategy(maxLeverage: number): StrategyParams {
  return {
    maxLeverage,
    riskPerTrade: 0.02,
    // ... 其他策略参数
  };
}
```

## 总结

这个 AI 加密货币自动交易系统是一个功能完善、架构清晰的自动化交易平台。通过理解核心模块和交易流程，你可以快速上手开发，并根据需要进行定制和扩展。

关键要点：
1. **核心架构**：基于 VoltAgent 的 AI Agent 系统
2. **交易流程**：数据收集 → AI 分析 → 决策执行 → 风险监控
3. **风险管理**：完善的止损止盈机制
4. **可扩展性**：支持多策略、多交易所、多 AI 模型
5. **监控友好**：Web 仪表盘和详细的日志系统

祝你开发顺利！